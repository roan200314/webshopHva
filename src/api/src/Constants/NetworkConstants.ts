/**
 * Contains constants for network configuration.
 * @export
 * @const
 */
export const NetworkConstants: { DEFAULT_PORT: number } = {
    /** Default port for the application */
    DEFAULT_PORT: 3001,
};
