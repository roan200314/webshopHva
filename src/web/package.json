{
    "name": "@webshop/web",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "vite",
        "build": "tsc --noEmit && vite build",
        "preview": "tsc --noEmit && vite build --mode preview && vite preview"
    },
    "devDependencies": {
        "lit": "3.1.0",
        "glob": "10.3.12",
        "vite": "5.2.8",
        "vite-plugin-eslint": "1.8.1"
    }
}
